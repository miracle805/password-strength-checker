# Password Strength Checker 🔐

A simple Python-based password strength checker that evaluates the security of passwords using common authentication and security best practices.

---

## Features
- Checks minimum password length
- Detects presence of digits (0–9)
- Detects uppercase letters (A–Z)
- Detects lowercase letters (a–z)
- Detects special characters (e.g. !@#$%)
- Provides clear feedback (Weak / Medium / Strong)

---

## Technologies Used
- Python 3
- Regular Expressions (`re`)
- Linux Terminal (Kali Linux)

---

## Usage
Run the script in the terminal:
```bash
python password_checker.py

